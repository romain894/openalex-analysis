[build-system]
requires      = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openalex-analysis"
version = "0.15"
description = "A python library to download, analyse or plot articles, institutions, and other entities from the OpenAlex API"
readme = "README.md"
authors = [{ name = "Romain Thomas", email = "contact@romainthomas.net" }]
license = "GPL-3.0-only"
license-files = [
    "LICENCE.txt",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Intended Audience :: Science/Research",
]
keywords = ["OpenAlex", "Data analysis", "Scientific research", "Plotly"]
dependencies = [
    "numpy >= 1.21.5",
    "pandas >= 2.0.1",
    "plotly >= 5.15.0",
    "kaleido<1.0",
    "psutil >= 5.9.4",
    "pyalex >= 0.14",
    "tqdm >= 4.65.0",
    "requests",
    "pyarrow"
]
requires-python = ">=3.10"

[tool.setuptools]
#include-package-data = true
packages = ["openalex_analysis", "openalex_analysis.data", "openalex_analysis.analysis", "openalex_analysis.plot"]

[project.urls]
Homepage = "https://pypi.org/project/openalex-analysis/"
Repository = "https://github.com/romain894/openalex-analysis"
Documentation = "https://romain894.github.io/openalex-analysis/html/index.html"
